---
title: Home
description: A progression-tree (skill tree) engine for educational apps and games. Data first, render second.
template: splash
hero:
  title: Yggdrasil Forge
  tagline: "Define progression as data. Validate it. Generate it. Render it anywhere. — skill trees · curricula · tech trees · RPGs"
  actions:
    - text: Try the editor
      link: https://fraga-labs.github.io/yggdrasil-forge/app/
      icon: rocket
    - text: Start here
      link: comeza/instalacion/
      icon: right-arrow
      variant: secondary
    - text: See the examples
      link: exemplos/galeria/
      variant: minimal
    - text: GitHub
      link: https://github.com/fraga-labs/yggdrasil-forge
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components'
import HeroRender from '../../../components/HeroRender.astro'
import InlineSvg from '../../../components/InlineSvg.astro'
import arquitectura from '../../../assets/diagramas/arquitectura.svg?raw'

<HeroRender
  id="gaia-cards"
  alt="Constellation atlas — 12 nodes with logic-* icons and the Neon preset, rendered with ygg render"
/>

*The constellation atlas from the gallery: two fields in the JSON (`icon: "logic-…"`, `preset: "neon"`) and one command (`ygg layout`) — not a single pixel touched by hand.*

```bash
pnpm add @yggdrasil-forge/core @yggdrasil-forge/react   # renderer
pnpm add -D @yggdrasil-forge/cli                        # ygg (the data path)
```

<CardGrid stagger>
  <Card title="Data first" icon="document">
    A tree is a JSON document with a published schema. The engine (`@yggdrasil-forge/core`) evaluates unlocks, resources, tiers and effects; everything else reads that data. What we promise and what we don't: [the stable contract](contrato/contrato-estable/).
  </Card>
  <Card title="The editor" icon="pencil">
    Visual authoring with an Inspector, live validation, auto-layout with six engines, a cards view, a Play mode and a synchronized code panel.
  </Card>
  <Card title="The data path" icon="rocket">
    `ygg validate → layout → render`: an AI (or any pipeline) generates a complete, validated, dressed tree without opening the editor.
  </Card>
  <Card title="Galician first" icon="star">
    Born in Galician, documented in Galician, with a complete English translation. Customer zero: Oberón, the professions section of the GAIA educational app.
  </Card>
</CardGrid>

<InlineSvg html={arquitectura} alt="Architecture: JSON document → core → React renderer, Studio and CLI" />

*How it all fits: [the architecture guide](arquitectura/guia/) · [the lifecycle of a node](contrato/ciclo-de-vida/) · [the API reference](referencia-api/) generated from the TSDoc · [build your engine plugin](extension/plugin-motor/).*
